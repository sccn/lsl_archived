This is a simple LSL app made for testing mouse and screen clicks. It is farily trivial, but provides a simple template for writing your own LSL applications.

This will build on Windows using the provided Visual Studio porject. It will also build on Linux distros using make (makefile is included) provided that Qt and LSL are correctly installed and the paths are known.

